# 2026-09-14

## 死代码静态扫描第三轮：结论「无新增死代码」（纯静态，未在宿主机跑任何构建）

- 覆盖 14 个维度，全部为空：未使用 import（前/后端）、未调用 private/protected 方法、未使用
  private 字段、零调用 public 方法、孤儿文件、孤儿导出、注释掉的代码块、SQL 未引用表、
  CSS 未使用类、前端未使用 import、前端模块级局部声明零引用、未使用配置键。
- 复用脚本：`%TEMP%\dcscan\all.mjs`（A–I 主扫描）、`scan2b.mjs`（J 公共方法 / K SQL / L CSS）、
  `scan3.mjs`（M 前端未使用 import / N 前端局部声明）。均为纯正则文本分析，可直接重跑。
- 本轮逐条证伪的误报（下轮别再重复排查）：
  - `app/layout.tsx`、`app/page.tsx`、`vite.config.ts`、`next-env.d.ts`：vinext 实际入口与配置，
    `next-env.d.ts` 还在 `tsconfig.json` 的 `include` 里，均非孤儿。
  - `react-dom` / `react-server-dom-webpack` / `@vitejs/plugin-react`：源码零 import，但是
    **vinext 的 peerDependencies**（plugin-react 非 optional），必须留在 devDependencies。
  - 后端 `H2*Tool` 与 `Mock*Tool`：实现的是**不同接口**（CareGuide/RouteGuide/MyAppointment/
    MaterialPreparation vs Appointment/Travel/MaterialChecklist），都被 `FollowupAgentService`
    注入，不是两代重复。
  - 所有 mock 工具都是 `@Component`，Spring 按接口注入 → 静态引用计数为 0 属正常。
  - `LlmAnswerGenerator` / `LlmConversationPlanner` 是 `@Primary @Component`，非死代码。
  - `FactExtractor` 接口被 `RuleFactExtractor implements`，作扩展点保留（第二轮已确认）。
- 顺带发现的**非死代码**问题，未改动：
  1. `agent.conversation.idle-timeout-seconds`(600)、`agent.conversation.suggest-new-after-turns`(40)
     只在 `@Value` 里有默认值，`application.yml` 未登记 → 能跑但不可发现，建议补进 yml。
  2. `Mock*` 前缀名不副实：这些类构造同样注入 `JdbcTemplate`、读真实 H2，与 `H2*` 无实现差异，
     纯历史命名（06 号文档第十二节已记为历史遗留）。赛前不建议改名。
