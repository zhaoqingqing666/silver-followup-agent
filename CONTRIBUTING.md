# 团队开发约定

1. 从最新 `main` 创建分支：`feature/功能名`、`fix/问题名`、`docs/文档名`。
2. 一个 Pull Request 只处理一类问题，描述“改了什么、怎样验证、是否改接口”。
3. 页面组件不直接写后端规则；Controller 不写流程；模拟工具不决定下一步。
4. 禁止提交真实身份证、手机号、病历和 API Key。
5. 合并前至少由另一位成员检查，并更新对应记录文档。

推荐提交信息：`feat(frontend): add material checklist`、`feat(tool): add mock appointment query`、`docs: record confirmation contract`。
