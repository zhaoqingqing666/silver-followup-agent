# 复诊事项协同办理智能体异常处理设计

异常处理不是在页面上显示一句“操作失败”，而是从异常发现、影响隔离、状态保留、恢复执行到结果核验的一套闭环。本项目将异常分为表层异常和深层异常：表层异常是用户能够感知并通过补充、选择或修改解决的问题；深层异常是系统内部的权限、状态、并发、工具、模型和数据一致性问题。两层使用同一套任务状态关联，前端只展示用户需要理解的原因和选择，后端保留可定位、可恢复、可审计的技术信息。

异常处理遵循以下原则：第一，如实区分未执行、执行失败、部分完成和全部完成，不能用自然语言把失败改写为成功；第二，优先保留已经确认的业务结果和仍然有效的草稿，只清除被异常影响的下游数据；第三，任何恢复动作都不能绕过身份校验、确认门禁和执行前复核；第四，每种异常至少提供一个安全出口，包括重试、修改条件、保留原安排、停止办理或请求人工帮助。

## 一 异常分层与处理闭环

| 层级 | 异常类别 | 典型表现 | 主要处理位置 | 处理目标 |
| --- | --- | --- | --- | --- |
| 表层 | 输入与理解异常 | 信息缺失、医院名称有歧义、语音未识别清楚 | 对话和候选选择界面 | 让用户用最少操作补全或纠正信息 |
| 表层 | 业务条件异常 | 指定日期无号、日程冲突、重复预约、时间已过去 | 任务计划和异常卡 | 给出真实替代方案，由用户决定是否变更 |
| 表层 | 意图和流程异常 | 中途改医院或日期、暂时不办、误把停止任务说成取消预约 | 会话编排和确认卡 | 明确用户想改什么，避免扩大操作范围 |
| 深层 | 状态与授权异常 | 旧卡、重复确认、授权目标缺失、恢复后凭据不完整 | 确认服务和业务状态机 | 默认拒绝执行，重新生成与当前事实一致的确认 |
| 深层 | 工具与系统异常 | 超时、限流、服务端错误、数据库异常、非法返回 | 工具运行时和业务执行器 | 判断能否重试，隔离失败步骤并保留权威结果 |
| 深层 | 并发与一致性异常 | 确认时号源被占、重复点击、改期过程中目标状态变化 | 工具、事务和幂等控制 | 防止重复写入、错写对象或丢失原预约 |
| 深层 | 模型异常 | 模型不可用、输出非法、调用不存在工具、连续调用失控 | 模型适配器和运行时 | 回退到规则与模板，不让模型错误进入写链路 |
| 深层 | 身份与安全异常 | 越权代办、医疗越界、紧急表达、提示注入或敏感信息泄露风险 | 确定性安全规则和权限层 | 立即阻断危险动作，必要时暂停普通流程 |

所有异常按“发现—分类—隔离—恢复—核验—记录”处理。发现阶段读取参数校验、任务状态、工具返回和安全规则；分类阶段判断异常是否可由用户修正、是否可自动重试以及是否已经产生业务写入；隔离阶段停止受影响的后续任务并废止不再可信的确认；恢复阶段只执行必要步骤；核验阶段重新读取数据库或工具状态；记录阶段保存脱敏参数、错误类型、重试次数、最终状态和关联业务编号。异常消息本身不承担业务判定，数据库与工具回读才是最终依据。

## 二 表层异常及用户可见处理

表层处理强调“说清楚、选项少、不中断”。异常卡先用一句话说明发生了什么，再显示已保留内容和最多三个主要操作；技术错误码、堆栈和内部服务名不直接展示给老人。

| 异常情况 | 识别依据 | 用户可见说明 | 可选处理 | 状态保留与清理 |
| --- | --- | --- | --- | --- |
| 必要信息缺失 | 医院、科室、日期等必填字段为空 | “还需要确认复诊日期。” | 回答当前问题、返回修改、稍后继续 | 保留已经明确的字段，一次只追问一个主要字段 |
| 名称或指代有歧义 | 医院或科室匹配到多个候选，“那个预约”等指向不唯一 | 列出真实候选并说明需要选哪一个 | 选择候选、重新描述、取消本次操作 | 不写入猜测结果，不改变原草稿和原确认卡 |
| 日期或时间无效 | 日期已经过去、时间格式错误、相对日期无法唯一确定 | 复述当前业务日期并说明该时间不能使用 | 重新选择日期、查看附近日期 | 清除无效日期之后的号源和路线，保留医院科室 |
| 指定日期无号 | 号源工具返回空列表 | “这一天暂时没有可预约号源。” | 查看附近日期、修改医院或科室、稍后再查 | 保留原需求；替代日期只有在用户选择后才写入草稿 |
| 已选时段与日程冲突 | 日程工具返回冲突事项 | 显示冲突事项名称和时间，不只提示“有冲突” | 换时段、换日期、仍保留该时间 | 不修改原日程；若用户保留冲突，确认卡继续显示风险 |
| 已有相近预约 | 当前用户已有同科室或相近时间的有效预约 | 显示已有预约的日期、医院和科室 | 保留已有预约、返回修改、继续新建并再次确认 | 不自动覆盖或取消原预约 |
| 中途更改医院、科室或日期 | 新输入与当前草稿中的关键事实不同 | 说明哪些内容需要重新查询 | 确认修改、撤回修改、查看原计划 | 保留未受影响信息；清除旧号源、冲突、路线、材料和旧确认 |
| 停止当前办理 | 用户表达“不办了”“先到这里”，但未明确指向已有预约 | “已停止继续办理，已存在的预约仍保留。” | 查看已有事项、重新开始、结束会话 | 只清理未提交草稿，不取消数据库中的预约 |
| 查询或识别暂时失败 | 语音、图片或只读工具没有得到有效结果 | 说明本次未识别或未查到，不推测内容 | 重试、改用文字、手动选择、人工帮助 | 保留办理主流程和已有确认；失败的附件结果不作为业务事实 |
| 写操作未完成 | 提交、改期、取消、提醒或通知工具返回失败 | 明确说明“哪一步未完成”和“原安排是否保留” | 重试该步骤、返回修改、查看当前事项 | 先回读业务状态，再决定回到待执行、部分完成或原安排状态 |

当前模拟号源在原日期无号时向后查询 3 天，不包含原日期，也排除已经过去的时段。替代号源只是候选，不代表用户已经同意换日。发生日程冲突时，原有日程始终保留；用户明确选择“仍保留这个时间”后，系统才继续生成确认卡，并将冲突事项带到最终确认中。

## 三 深层异常及系统内部处理

深层处理的重点不是向用户展示更多技术细节，而是保证一次异常不会造成越权、重复写入、状态错位或成功结果丢失。系统内部采用“先阻断后恢复”的策略：只要无法证明授权、目标或业务状态仍然一致，就不继续执行写操作。

| 深层异常 | 判定方式 | 系统处理 | 一致性与恢复结果 | 当前落地情况 |
| --- | --- | --- | --- | --- |
| 模型输出不符合契约 | JSON 解析失败、工具名不在白名单、参数类型或枚举非法 | 拒绝该调用；能映射到既有安全流程时受控回退，否则明确不支持 | 不执行工具，不改变业务事实，不把模型草稿当结果 | 已实现 |
| 模型超时、限流或不可用 | 连接超时、429、5xx、空响应 | 对可恢复错误有限退避重试；重试用尽后切换规则规划器和固定模板 | 核心预约流程仍可继续，业务结果只取自工具 | 已实现基础重试与回退 |
| 模型连续调用或重复调用 | 达到连续调用上限，或工具名与参数完全重复 | 中止模型续跑，使用最近一次权威工具结果收口 | 防止循环消耗和重复查询，更不允许因此触发写操作 | 已实现 |
| 确认凭据过期或重复使用 | `confirmationId` 不匹配、已消费、动作类型不可识别 | 拒绝执行并提示重新检查计划 | 原业务数据不变；需要写入时重新签发确认卡 | 已实现 |
| 授权目标或草稿不完整 | `confirmationKind`、完整目标集合或执行所需草稿缺失 | 按失败关闭原则整份作废，不按残留字段执行 | 避免“确认 A、执行 B”以及批量目标缩水 | 已实现 |
| 执行前业务事实变化 | 时段已过去、号源被占、预约已取消或不属于当前用户 | 在写入前重新校验；失败后返回重新选择或当前真实状态 | 不提交失效对象，不沿用旧确认 | 已实现于核心预约和取消路径 |
| 重复点击或请求重放 | 同一凭据再次提交、稳定业务标识已存在 | 一次性消费确认凭据，并由数据库约束或稳定标识阻止重复写入 | 返回现有结果或“已办理”，不产生第二条预约或提醒 | 已实现核心保护 |
| 数据库或业务工具异常 | 工具抛出异常、事务失败、返回结构缺失 | 记录失败轨迹，停止依赖该结果的后续步骤；清除不可继续使用的确认 | 未写入则进入 `TOOL_ERROR`；已预约则进入 `PARTIAL` | 已实现 |
| 预约成功但提醒或通知失败 | 预约编号已生成，后续工具未全部成功 | 固化预约成功事实，标记具体未完成项 | 重试只补办失败事项，不再次提交预约 | 已实现 |
| 改期过程中失败 | 新号源不可用或改期提交失败 | 保留原预约，只有新方案成功后才更新关联提醒和通知 | 用户不会因一次失败失去已有安排 | 已实现核心路径 |
| 会话恢复或快照不兼容 | 恢复后缺少完整授权三元组或必要草稿 | 恢复可读状态，但废止不可信确认，要求重新确认 | 已完成业务保留；未完成写操作不自动续做 | 已实现 |
| 身份或服务对象不匹配 | 操作者与服务对象无照护关系，目标记录归属不符 | 在工具执行前拒绝，模型和前端参数不能覆盖后端注入身份 | 不返回越权数据，不产生代办写入 | 已实现于模拟关系校验 |
| 医疗越界或明显紧急表达 | 确定性关键词和边界规则命中 | 医疗越界停止推断；紧急情况优先暂停普通办理并废止待确认授权 | 返回专业求助提示，避免误触原操作 | 已实现规则保护 |
| 真实外部平台状态未知 | 请求超时但无法判断对方是否已受理 | 使用业务请求号查询或对账，未查清前不盲目重发 | 显示“结果待确认”，由查询、补偿任务或人工处理收口 | 正式接入医院和通知平台后补充 |
| 持续故障或集中失败 | 同一工具在时间窗口内连续失败 | 熔断故障工具，暂停自动重试并切换人工渠道；恢复后再探测 | 防止故障放大，保留待办队列和审计记录 | 正式部署后补充 |

## 四 重试 降级与人工接管

系统不对所有失败统一重试。业务条件不满足时，重复调用不会产生新结果，应让用户修改条件；网络和限流等暂时性故障可以有限重试；鉴权、参数和权限错误继续重试只会放大问题，应立即停止。写操作在结果未知时不能直接重发，必须先用业务编号回查。

| 失败类型 | 是否自动重试 | 处理策略 | 面向用户的最终状态 |
| --- | --- | --- | --- |
| 网络超时、429、服务端 5xx | 可以，次数受限并退避 | 保持同一请求语义；超过上限后降级或转人工 | “暂时未完成，信息已保留，可稍后重试” |
| 参数错误、无权限、鉴权失败 | 不重试 | 拦截请求，修正参数或权限配置；不向用户暴露密钥和内部信息 | “当前操作不能执行，请修改或寻求帮助” |
| 无号、冲突、重复预约 | 不自动重试 | 查询替代方案，把决定权交给用户 | “原条件不满足，请选择其他方案或保留原安排” |
| 模型不可用或输出非法 | 模型侧有限重试，随后降级 | 切换规则规划器、模板回答和只读权威结果 | “仍可继续办理，表达可能更简洁” |
| 写操作失败且确认未写入 | 可在重新核验后重试 | 保留草稿，重新检查号源、身份和确认有效性 | `TOOL_ERROR`，显示重试或修改入口 |
| 写操作已成功但响应丢失 | 不直接重发 | 按请求号、预约号或稳定标识回查并返回已有结果 | “已查明办理结果”，避免重复写入 |
| 次要任务失败 | 只重试失败节点 | 保留主业务成功结果，将失败节点放入补办队列 | `PARTIAL`，列出已完成和待补办事项 |
| 连续失败或无法判断 | 停止自动处理 | 生成脱敏问题摘要、当前状态和已完成事项，交由人工核查 | “已停止自动重试，需要人工帮助” |

人工接管时只提供完成任务所需的最少信息，包括服务对象标识、当前阶段、已完成事项、失败步骤、最后一次错误类型和业务编号。语音原文、图片、完整手机号和模型上下文不应默认全部转交。人工处理完成后还要回写结果状态，避免人工已经解决而系统仍反复提示失败。

## 五 部分成功 补偿与状态恢复

复诊办理包含多个先后依赖的写操作，不能把它们简单视为一个“全成或全败”的动作。预约是主业务，提醒、材料初始化和家属通知是关联任务。主业务一旦成功，后续异常应采用前向恢复，即继续完成未完成事项，而不是撤销预约并从头执行。

| 场景 | 已确认事实 | 失败后的处理 | 禁止的处理 | 恢复终点 |
| --- | --- | --- | --- | --- |
| 预约未成功 | 没有预约编号 | 保留草稿并重新查号、重新确认 | 创建提醒或声称已经预约 | 回到选号或 `TOOL_ERROR` |
| 预约成功，提醒失败 | 预约编号和时段有效 | 保留预约，仅补建缺失提醒 | 再次提交预约 | `PARTIAL → COMPLETED` |
| 预约成功，家属通知失败 | 预约有效，通知状态为未完成 | 重新生成通知或让用户选择不再通知 | 宣称已经送达或回滚预约 | `PARTIAL → COMPLETED` |
| 取消失败 | 原预约仍为有效状态 | 明确“取消未完成，原预约保留”，重新确认后再试 | 把页面卡片直接改成已取消 | 返回原预约事项 |
| 改期失败 | 原预约仍有效 | 保留原预约并释放无效新方案，允许重选 | 先取消原预约再尝试新号源 | 返回原安排或重新选时段 |
| 用户修改关键条件 | 新条件尚未确认 | 只清除受影响的候选、冲突、路线、材料和旧授权 | 删除已存在的原预约 | 重新规划并生成新确认 |
| 会话关闭或长时间未操作 | 已完成结果仍在数据库 | 关闭会话后只读查看；空闲恢复时重验待确认内容 | 因会话结束删除业务记录 | 历史可查或重新确认 |

当前原型使用单实例和单个 H2 数据库，通过顺序执行、阶段状态、事务、一次性确认凭据和完成标记实现恢复，不宣称已经解决跨医院、地图、日历和消息平台的分布式事务。接入真实平台后，需要为每次写操作增加稳定请求号、结果查询接口、补偿任务、失败队列、告警和人工对账；通知状态也应拆分为已创建、已发送、已送达和已读，不能仅凭接口返回成功就声称对方已经收到。

## 六 异常结果记录与验收

异常记录用于恢复和审计，不用于向老人展示内部细节。每条记录至少关联会话、服务对象、任务节点、工具名称、脱敏参数、异常类别、是否产生写入、重试次数、最终状态和业务编号。日志不保存 API 密钥，不直接记录图片或音频正文，错误信息对前端输出前要转换为简短、可操作的话术。

| 验收场景 | 通过条件 |
| --- | --- |
| 信息不完整 | 系统只追问缺失字段，已知信息不丢失，不出现虚构结果 |
| 指定日期无号 | 显示真实替代号源，未选择前不改日期、不生成确认 |
| 日程冲突 | 原日程不被修改，用户保留冲突时确认卡仍显示冲突 |
| 确认后号源被占 | 不创建预约，旧确认失效并返回重新选号 |
| 连续点击确认 | 最多产生一次业务写入，后续请求返回已办理或凭据失效 |
| 预约后提醒失败 | 预约保持成功，状态为部分完成，补办不重复预约 |
| 模型断开或输出非法 | 回退后仍能继续核心流程，不能编造工具结果 |
| 越权代办 | 查询和写入均被拒绝，不泄露其他老人的信息 |
| 紧急或医疗越界表达 | 普通办理被安全规则接管，不输出诊断、用药或治疗建议 |
| 会话恢复 | 已完成事项可查，不完整或旧确认不能继续执行 |
